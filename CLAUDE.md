# Minecraft 직업 밸런스·코드 검토 지침

## 1. 기본 환경

* Minecraft Java `26.3`, Fabric 기준.
* GitHub 저장소: `ignitebc/MC-Code`
* 기본 브랜치: `26.3`
* 별도 지시가 없으면 최신 `26.3` 기준으로 분석한다.

## 2. 코드 확인 원칙

답변 전 대상 기능의 실제 호출 관계를 GitHub에서 확인한다.

필요에 따라 함께 확인한다.

* 클래스 / 인터페이스 / Mixin
* Event / Trigger / Reward / Condition
* Manager / Config
* JSON / Datapack / Lang
* Packet / 저장·로드 코드

일부 파일만 보고 동작을 추측하지 않는다.

GitHub 접근 실패 시 확인하지 못한 범위와 파일을 명확히 적고 임의로 작성하지 않는다.

## 3. 코드 수정 답변

코드 수정안을 제공할 경우:

* 수정 대상 파일 목록을 먼저 적는다.
* 수정되는 Java 클래스와 JSON 등 설정 파일은 전체 내용을 제공한다.
* `...기존 코드...` 같은 생략을 사용하지 않는다.

사용자가 명시하지 않으면 GitHub 파일 수정, 브랜치 생성, commit, push, merge, PR 생성은 하지 않는다.

---

# Minecraft 직업 밸런스 30-Agent 시뮬레이션 지침서

## 4. 밸런스 목표

서버는 약 **30일 운영**한다.

직업 1개를 주력으로 하루 약 1시간씩 정상 플레이했을 때:

**29~30시간 내 해당 직업의 모든 스킬을 실제 구매 완료**

해야 한다.

완료는 다음을 모두 만족한 시점이다.

* Required Level 충족
* Parent 조건 충족
* 필요한 직업코인 확보
* 모든 스킬 실제 구매 완료

BTC는 준비·이동·탐색·재보급을 포함한 장기 평균:

**약 6 BTC/h 중심, 허용범위 6~10 BTC/h**

를 목표로 한다.

EXP와 BTC는 별도로 분석·조정한다.

### 직업 등급별 성장 설계

등급은 최종 완료시간의 차이가 아니라 **초반·중반·후반 성능 곡선의 차이**를 의미한다.

모든 직업은 최종적으로 약 29~30시간에 모든 스킬을 해방하는 것을 목표로 하되 성장 체감은 다음과 같이 다르게 설계한다.

#### A등급

대상:

* 농부
* 대장장이
* 연금술사

성격:

**초반은 좋지 않지만 성장할수록 성능이 크게 증가하고 후반에 높은 고점이 나타나는 직업**

원칙:

* 초반 EXP/BTC 효율은 상대적으로 낮아도 허용한다.
* 중반부터 효율이 눈에 띄게 상승해야 한다.
* 후반은 B/C등급보다 명확히 높은 Action/h, EXP/h 또는 BTC/h 고점이 나타나야 한다.
* 초반이 약하다는 이유로 전 구간을 동일 배율로 상향하지 않는다.
* 후반 핵심 스킬의 성장 체감이 분명해야 한다.

#### B등급

대상:

* 낚시꾼
* 사냥꾼

성격:

**초반은 평범하거나 다소 약하지만 중반부터 성능이 좋아지는 직업**

원칙:

* 초반부터 지나치게 높은 효율을 주지 않는다.
* 중반 핵심 스킬 획득 이후 Action/h와 EXP/BTC 효율이 확실히 증가해야 한다.
* 후반은 중반보다 강하지만 A등급만큼 큰 고점은 요구하지 않는다.
* 초반→중반의 체감 성장이 가장 중요하다.

#### C등급

대상:

* 광부
* 굴착가
* 모험가

성격:

**초반부터 후반까지 큰 고점 없이 비교적 일정하고 무난한 성능을 유지하는 직업**

원칙:

* 초반이 지나치게 약하지 않아야 한다.
* 중후반 스킬을 얻어도 성능이 폭발적으로 증가하지 않는다.
* 초·중·후반 EXP/h와 BTC/h가 비교적 평탄해야 한다.
* 특정 후반 스킬 하나가 전체 효율을 지나치게 끌어올리면 C등급 설계 위반으로 본다.

등급 차이 때문에 최종 완료시간 자체를 크게 다르게 만들지 않는다.

**A/B/C 모두 최종 목표는 약 29~30시간이며, 차이는 그 시간에 도달하는 성장곡선이다.**

---

# 5. 코드 및 데이터 기준

시뮬레이션 전 최신 `ignitebc/MC-Code`의 `26.3`을 확인한다.

직업마다 실제 코드에서 다음을 확인한다.

* 모든 Action과 조건
* 행동별 EXP
* BTC 확률과 지급량
* 레벨별 필요 EXP
* 레벨업당 직업코인
* 스킬 가격·Required Level·Parent 조건
* EXP 배율
* 속도·범위·추가 드롭·자동화 효과

과거 분석값을 그대로 사용하지 않는다.

시즌 실측 로그가 있으면 Action/h, EXP/h, BTC/h 검증에 사용하며 코드와 충돌하면 원인을 먼저 확인한다.

본섭 실측은 별도 지시가 없으면 **2026-08-07 서버 오픈 이후 데이터**를 기준으로 한다.

---

# 6. 기본 시작 조건

모든 Agent는 동일하게 시작한다.

* 직업 Lv.1, EXP 0, 스킬 미보유
* 쿠폰·Beacon·Elytra·인챈트 없음
* 자동화 시설·다른 플레이어 지원 없음

기본 장비:

* 다이아몬드 방어구 세트
* 해당 직업의 다이아몬드 도구

다이아 도구가 없는 직업은 일반 장비를 사용한다.

예:

* 낚시꾼: 일반 낚싯대
* 연금술사: 기본 양조대
* 대장장이: 모루·숫돌·화로 등 기본 작업대

씨앗, 연료, 청금석, 유리병, 블레이즈 가루, 네더 사마귀 등 소비재를 무한 지급하지 않는다.

도구 내구도도 감소시키며 파손 시 재료 확보와 재제작 시간을 포함한다.

---

# 7. 30명 Agent 시뮬레이션

직업마다 **30명의 독립 Agent**를 실행하며 각 Agent는 다른 Random Seed를 사용한다.

개인차에 포함:

* 이동·탐색 효율
* 몬스터·광물 발견 운
* Loot/BTC RNG
* 낚시 입질·반응속도
* 작물 성장·자원 확보
* 인벤토리 정리
* 제작·재보급
* 스킬 구매 순서
* 정상 범위의 플레이 비효율

기준은 **숙련된 일반 플레이어**다.

고의적 잠수나 초보 수준 비효율은 제외하지만 TAS 수준 이동·전투·미래 RNG 예측도 허용하지 않는다.

---

# 8. 세션 및 실제 시간 계산

기본은 **1시간 × 최대 30일**이다.

30시간 내 완료하지 못하면 실제 완료시간 확인을 위해 계속 실행한다.

세션 종료 후 다음은 유지한다.

* 레벨·EXP·직업코인·스킬
* 아이템·장비 내구도
* 농장·작업장·확보 자원·탐색 지역

로그아웃 중 청크가 계속 로드된다고 가정하지 않는다.

시간당 행동량을 `3600 ÷ 행동시간`으로 계산하지 않는다.

반드시 다음 전체 루프를 계산한다.

**준비 → 이동 → 탐색/재료 확보 → 핵심 행동 → 회수 → 정리 → 재보급**

대기 중 다른 작업이 가능하면 시간을 이중 계산하지 않는다.

---

# 9. 성장 및 스킬 구매

처음부터 최종 스킬 성능을 적용하지 않는다.

`없음 → I → II → ... → X`

순서대로 실제 구매한다.

스킬 구매 이후부터 다음 효과를 적용한다.

* EXP 배율
* 채굴·이동·공격·낚시 속도
* 범위 증가
* 추가 드롭
* 재료 절약·자동 재파종 등

30명 모두 같은 구매순서를 사용하지 않는다.

EXP·작업효율 스킬을 우선하는 경향은 허용한다.

직업마다 **최적화 Agent 1명**도 실행해 지나치게 빠른 성장 루트를 확인한다.

---

# 10. EXP 및 BTC 계산

행동별 EXP는 최신 실제 코드 값을 사용한다.

추가 아이템이 발생해도 코드상 Action이 1회면 EXP도 1회만 지급한다.

EXP 배율은 구매 시점 이후에만 적용한다.

BTC는 행동마다 실제 확률로 독립 RNG를 실행한다.

1% 행동 100회를 정확히 1 BTC로 계산하지 않는다.

기본 테스트에서는 EXP/BTC 쿠폰을 제외한다.

---

# 11. 직업별 실제 시뮬레이션

## 연금술사

**재료 준비 → 네더 이동 → 요새 탐색 → 블레이즈 전투 → Blaze Rod 확보 → Nether Wart 확보 → 귀환 → 재배 → 병·물·부재료 확보 → 양조 → 회수 → 재보급**

Fortress 위치를 미리 알지 못하며 재료 부족 시 실제 재확보 과정을 거친다.

A등급이므로 초반 준비과정은 느릴 수 있으나 중후반 양조·재료 관련 스킬 이후 높은 고점이 나타나야 한다.

## 굴착가

**굴착지 탐색 → 이동 → 자연 블록 파괴 → 회수 → 정리 → 다음 작업지역 이동 → 귀환·재보급**

블록 사이 이동과 삽 내구도를 포함한다.

설치→파괴 반복은 악용 테스트로 분리한다.

C등급이므로 초·중·후반 효율 차이가 과도하지 않아야 한다.

## 농부

**씨앗 확보 → 물 확보 → 소형 농경지 → 파종 → 성장 → 수확 → 재파종 → 씨앗 증식 → 농지 확장**

처음부터 대형 농장을 제공하지 않는다.

Auto Replant, Range Harvest는 구매 이후 반영한다.

A등급이므로 초반 소형 농장은 약하지만 후반 대형 농장과 핵심 스킬 조합에서 높은 고점이 나타나야 한다.

## 낚시꾼

**낚시터 탐색 → Open Water 확인 → 캐스팅 → 입질 대기 → 반응 → 회수 → Loot RNG → 정리 → 재캐스팅**

내구도, 재제작, 훈연과 연료도 반영한다.

B등급이므로 초반보다 낚시속도 등 중반 핵심 스킬 획득 이후 효율 증가가 명확해야 한다.

## 사냥꾼

**사냥터 이동 → 자연 스폰 탐색 → 접근 → 공격 → 처치 → 회수 → 회복 → 다음 탐색**

몹별 체력·타격수·위험도와 장소 탐색시간을 반영한다.

몬스터 공장·스포너는 악용 테스트로 분리한다.

B등급이므로 공격·이동 관련 스킬이 갖춰지는 중반부터 성장 속도가 크게 좋아져야 한다.

## 광부

**광산 탐색 → 지하 이동 → 일반 블록 채굴 → 광석 탐색 → 광맥 채굴 → 회수 → 다음 탐색 → 정리 → 귀환**

Stone/Deepslate, Cave, 용암, 횃불, 비보상 블록, 내구도를 포함한다.

C등급이므로 희귀광석이나 후반 스킬 때문에 효율이 폭발하지 않고 전 구간 비교적 일정해야 한다.

## 대장장이

**작업 확인 → 부족 재료 확보 → 연료 확보 → 제련 → 제작 → 모루·숫돌·마법부여 → 정리**

재료와 Minecraft EXP를 무한 제공하지 않는다.

준비재료 burst EXP/h와 지속가능 EXP/h를 분리하며 후자를 기준으로 한다.

A등급이므로 초반 재료 확보는 느리더라도 후반 제작·제련·강화 루프에서 높은 고점이 발생해야 한다.

## 모험가

**탐험 준비 → 이동 → 지형 우회 → 구조물·지역 탐색 → 회수 → 계속 탐험 또는 귀환**

최대 이동속도×시간으로 계산하지 않는다.

다른 직업 수행 중 발생하는 이동 EXP/BTC도 교차검증한다.

C등급이므로 전 구간 비교적 일정한 효율을 유지하고 이동속도 스킬 때문에 후반 수익이 폭발하지 않아야 한다.

---

# 12. 초반·중반·후반 분석

직업별로 초반·중반·후반을 나누어 계산한다.

* 행동/h
* 기본·실제 EXP/h
* BTC/h
* 주요 활성 스킬
* 시간 사용 비율
* 주요 병목

등급별로 다음 성장곡선을 확인한다.

* **A:** 초반 낮음 → 중반 상승 → 후반 큰 고점
* **B:** 초반 보통 이하 → 중반부터 상승 → 후반 유지·소폭 상승
* **C:** 초반 → 중반 → 후반 변화폭이 작고 비교적 평탄

완료시간이 29~30시간이더라도 해당 등급 성장곡선과 크게 다르면 별도 조정한다.

---

# 13. 최적 루트 및 악용 테스트

30명과 별개로 직업별 최적 Agent를 실행한다.

자동화, 버그, TAS, 미래 RNG 예측은 금지한다.

별도 검증:

* 블록 설치·파괴 반복
* 자동농장·자동낚시·자동제련·자동양조
* 몬스터 공장·스포너
* 준비된 재료 대량 제작
* EXP 중복
* 이동 Action 반복
* 직업 간 패시브 중복

악용 결과는 기본 30명 평균에 포함하지 않는다.

---

# 14. 시즌 실측 데이터 교차검증

시즌 로그가 있으면 다음을 비교한다.

* Action/h
* EXP/h
* BTC/h
* 직업 레벨·스킬 수
* 실제 플레이시간

기본 본섭 분석 시작일은 **2026-08-07**로 한다.

시뮬레이션과 차이가 크면 AFK, 쿠폰, 준비 재료, 자동화, 다직업 성장, Metrics 기간 차이 등을 먼저 확인한다.

---

# 15. 결과 출력

| 직업 | 등급 | 평균 완료시간 | 중앙값 | P10 | P90 | 초반 EXP/h | 중반 EXP/h | 후반 EXP/h | BTC/h | 판정 |
| -- | -- | ------: | --: | --: | --: | -------: | -------: | -------: | ----: | -- |

추가로:

* 초·중·후반 BTC/h
* 주요 병목
* 스킬 구매 타임라인
* 시간 구성
* 최적 Agent 완료시간
* 악용 위험
* 등급 성장곡선 적합 여부

를 제공한다.

---

# 16. 합격 기준

### 스킬 해방

30명 평균과 중앙값이 모두 **29~30시간**이어야 한다.

### 등급 성장곡선

완료시간과 별도로 A/B/C 성장 특성을 만족해야 한다.

* A등급: 후반 고점이 명확해야 함
* B등급: 중반부터 체감 상승해야 함
* C등급: 전 구간 큰 고점 없이 비교적 평탄해야 함

### BTC

장기 평균은 기본적으로 **6~10 BTC/h** 범위에서 평가한다.

등급별 체감 차이는 초·중·후반 분포로 만들며, 단순히 A등급이라는 이유로 전체 경제량을 무제한 증가시키지 않는다.

---

# 17. 수치 조정 절차

1. 최신 코드 확인
2. 현재 값으로 30명 시뮬레이션
3. 초·중·후반 분석
4. A/B/C 성장곡선 적합 여부 확인
5. 최적 루트·악용 확인
6. 시즌 실측과 비교
7. EXP와 BTC 문제 분리
8. 문제 Action 또는 성장구간만 수정
9. 30명 전체 재시뮬레이션
10. 완료시간과 등급곡선 모두 재검증

전 구간에 동일 배율을 곱해 등급 특성을 없애지 않는다.

---

# 18. 금지사항

* 코드 미확인 상태에서 과거 분석값 사용
* 최종 스킬을 처음부터 적용
* 무한 재료·내구도 가정
* 이동·탐색·성장·입질·제련·양조시간 삭제
* 광석만 연속 채굴
* 몬스터 즉시 연속 스폰
* 처음부터 대형 농장 제공
* 자동화를 정상 플레이로 계산
* BTC를 확률 기대값으로 고정 지급
* Parent 조건·직업코인 무시
* 평균만 보고 합격 판정
* 등급별 성장곡선 무시
* 수정 후 재시뮬레이션 생략

---

## 핵심 기준

**모든 직업은 정상 플레이 기준 약 29~30시간에 모든 스킬 구매 완료**

**A등급은 후반 고점, B등급은 중반 성장, C등급은 전 구간 평탄한 성능을 갖도록 설계**

**BTC는 실제 장기 플레이 기준 6~10 BTC/h 범위에서 관리**



## 19. Git Commit 관련

### 19.1 기본 원칙

* 사용자가 **명시적으로 commit을 지시하기 전까지 절대 임의로 commit하지 않는다.**
* 코드 수정이 완료되었더라도 commit 지시가 없으면 작업 파일만 수정한 상태로 유지한다.
* commit뿐 아니라 push도 사용자의 명시적인 지시가 있을 때만 수행한다.

### 19.2 Commit 수행 전 확인

사용자가 commit을 지시한 경우 다음 사항을 먼저 확인한다.

1. 현재 작업 브랜치를 확인한다.
2. 기존 Git commit 이력을 확인한다.
3. 기존 저장소의 commit 메시지 형식과 작성 방식을 확인한다.
4. 현재 변경 파일과 변경 내용을 확인한다.
5. 서로 다른 기능이 하나의 commit에 섞여 있지 않은지 확인한다.
6. 기존 변경사항이나 다른 작업자의 변경사항을 임의로 포함하지 않는다.

### 19.3 Commit 단위

* commit은 **기능별로 분리**한다.
* 서로 독립적으로 되돌릴 수 있는 변경은 각각 별도 commit으로 작성한다.
* 하나의 commit에 서로 관련 없는 기능을 묶지 않는다.
* 추후 `revert`, `cherry-pick`, 원인 추적이 쉽도록 가능한 한 논리적인 최소 단위로 나눈다.

예:

* 총기 데이터 추가 → 별도 commit
* 총기 사운드 시스템 변경 → 별도 commit
* UI 수정 → 별도 commit
* 문서 수정 → 필요하면 별도 commit

### 19.4 Commit 메시지 형식

Commit 메시지는 다음 형식을 사용한다.

```text
<type>: <제목>

- 변경 내용 1
- 변경 내용 2
- 변경 내용 3
```

작성 규칙:

* 첫 줄에는 `type: 제목` 형식으로 작성한다.
* 제목은 변경 내용을 한눈에 알 수 있도록 간결하게 작성한다.
* 상세 내용은 **1~3개**만 작성한다.
* 상세 내용은 반드시 `-` 불릿 기호를 사용한다.
* 불필요하게 긴 설명은 작성하지 않는다.
* 커밋 메시지 마지막에 불필요한 빈 행이나 공백을 남기지 않는다.
* commit 실행 직전에 최종 메시지 전체를 다시 확인한다.

### 19.5 Commit Type

| Type       | 용도                          | 예시                              |
| ---------- | --------------------------- | ------------------------------- |
| `feat`     | 새로운 기능 추가                   | `feat: 위험성평가 검색 필터 기능 추가`       |
| `fix`      | 오류 또는 버그 수정                 | `fix: 작업단계 저장 시 빈 값 처리 오류 수정`   |
| `docs`     | 문서 수정                       | `docs: 커밋 메시지 작성 기준 문서 수정`      |
| `style`    | 코드 포매팅, 들여쓰기 등 동작에 영향 없는 변경 | `style: 코드 포매팅 정리`              |
| `design`   | UI/UX 및 디자인 변경              | `design: 작업단계 목록 버튼 UI 개선`      |
| `test`     | 테스트 코드 추가 또는 수정             | `test: 위험성평가 저장 로직 테스트 코드 추가`   |
| `refactor` | 기능 변경 없는 코드 구조 개선           | `refactor: 작업단계 데이터 처리 로직 리팩토링` |
| `build`    | 빌드 설정 및 의존성 관련 변경           | `build: 빌드 설정 파일 수정`            |
| `ci`       | CI/CD 설정 및 배포 스크립트 변경       | `ci: GitLab CI 배포 스크립트 수정`      |
| `perf`     | 성능 개선                       | `perf: 목록 조회 성능 개선`             |
| `chore`    | 기타 설정 및 유지보수 작업             | `chore: .gitignore 설정 추가`       |
| `rename`   | 파일, 폴더, 클래스 등의 이름 변경        | `rename: 작업단계 관련 폴더명 변경`        |
| `remove`   | 사용하지 않는 코드 또는 파일 제거         | `remove: 사용하지 않는 컴포넌트 파일 삭제`    |

### 19.6 Commit 예시

```text
feat: 위험성평가 검색 필터 기능 추가

- 작업명과 위험요인 기준 검색 기능 추가
- 검색 조건 초기화 기능 추가
- 기존 목록 조회 로직과 검색 조건 연동
```

서로 다른 기능이 포함되어 있다면 하나로 묶지 않고 각각 별도 commit으로 분리한다.

### 19.7 금지사항

* 사용자 지시 없이 임의로 commit하는 행위
* 사용자 지시 없이 임의로 push하는 행위
* 서로 관계없는 여러 기능을 하나의 commit으로 묶는 행위
* 기존 Git 이력을 확인하지 않고 임의의 형식으로 commit 메시지를 작성하는 행위
* 다른 작업자의 변경사항을 확인 없이 함께 commit하는 행위
* 제목만 작성하고 변경 내용을 생략하는 행위
* 상세 내용을 과도하게 많이 작성하는 행위
* commit 메시지 끝에 불필요한 빈 행이나 공백을 남기는 행위
