apply plugin: 'com.gradleup.shadow'

architectury {
    common rootProject.enabled_platforms.split(",")
}

loom {
    accessWidenerPath = file("src/main/resources/yamlconfig.accesswidener")
}

dependencies {
    // We depend on fabric loader here to use the fabric @Environment annotations and get the mixin dependencies
    // Do NOT use other classes from fabric loader
    implementation "net.fabricmc:fabric-loader:${project.fabric_loader_version}"
    implementation "dev.architectury:architectury:${project.architectury_version}"

    implementation "org.snakeyaml:snakeyaml-engine:2.7"
    implementation files(rootProject.file("../UILib-26.2/fabric/build/libs/uilib-2.1.4-26.2-fabric.jar"))
}
